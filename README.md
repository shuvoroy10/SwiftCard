1.Ans => null has a value , undefined hasn't. And both are false.
2.Ans => forEach used to change array element but not new array in same array , Map used to Change array element with new array . filter used to search a element by condition in array.
3.Ans => Arrow function is a shorter way to write functions in ES6. const square = x => x * x; here automatically returns the value. But if i use { }, i must  write return.
4.Ans => we use async for functional waiting.
5.Ans => Global Scope – accessible anywhere in the code.
Function Scope – accessible only inside the function.
Block Scope – accessible only inside { } (like if, for blocks).
